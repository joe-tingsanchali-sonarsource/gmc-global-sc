apply plugin: "org.sonarqube"

sonarqube {
    properties {
        // Insert other SonarQube properties here
        property 'sonar.projectName', 'gmc-global'
        property 'sonar.projectKey', 'gmc-global'
    }
}
